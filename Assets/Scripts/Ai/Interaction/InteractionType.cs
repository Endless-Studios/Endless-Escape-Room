namespace Ai
{
    /// <summary>
    /// Enum used for defining specific interactions between the Ai and an object in the scene. Normally used to define
    /// the related animations.
    /// </summary>
    public enum InteractionType
    {
        None,
        Default,
        OpenDoor
    }
}